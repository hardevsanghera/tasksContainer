apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: nginx
  name: nginx
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
  selector:
    io.kompose.service: nginx
